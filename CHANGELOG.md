# Changelog

## 0.0.1

### 2025-2-12

#### Features

* 重新添加并改进工具提示自动缩进功能, 它现在不会随着光标轻微移动而改变，显示更稳定。
* 工具提示现在会在渲染时合并多个连续空行为一个，并删除末尾空行。便于不同工具提示组件的格式化书写。
  * 在工具提示中的换行符 `\n` 会进行强制换行，其分隔的两个字符串即使没有字符也不会被视为空行。
* 添加了 REI 相关 mod 作为运行测试 mod。以后的版本确保与其的兼容性。
* 添加了 Changelog。


#### Refactored

* 在工具的能量工具提示和心的可用功能提示后面添加空行。
* 使用于运行测试的 mod 从 web 上下载而非携带在仓库目录内，减小仓库文件体积。

#### Fixed

* 修复了本地运行测试 mod 会随着 build 而一起打包的问题。

### 2025-2-13 - 2025-2-14

#### Features

* 添加了测试物品，用于功能的测试
* 添加了 iwtest_getnumber 和 iwtest_setnumber 命令，用于在游戏内测试数据。
  * 二命令会操作一个内置整型列表，该列表用于配置需要测试项的数据，不会随着游戏保存。
  * 将需要测试的数据替换为列表参数，该数据便可以在游戏内通过指令更改，而无需重新编译。
* 将每种未携带能力的心加入了能力物品组，以便于附魔等功能的测试。
* 完善能力数字的显示功能。
  * 现在可以显示一个字符串，并且显示在恰当的位置。
* 添加了基础心绿宝石心。作为 4 级钻石心的一个替代，它拥有更低的附魔能力。
* 为工具重新添加了修复材料。 现在烈焰棒子使用烈焰棒和烈焰粉修复。
  * 烈焰棒子的修复由 `can_repair_blazerod` 标签控制。
* 现在物品标签生成可以支持标签嵌套。

#### Changed

* 现在所有的基础心都实现自 `BaseHeart`，可以向其构造函数传入不同参数来简便的实现不同的基础心。
* 将世界等级从 0 - 8 级别重新更改到 0 - 10 级，并为每个等级添加其对应颜色。 
  * 现在等级颜色不再与心的颜色绑定，配色可参考 `assets/level_color_reference.png`。
  * 虚空合金心暂时支撑 0 - 10 级以适应该变更。
* HeartDataFlag 现在使用 4 位材料等级位，并且支持 0 级材料等级。因此之间版本的心与该版本不兼容。
* 现在 RandomEnchantGenerator 在附魔失败时可以根据配置得到不同的心，而非总是默认的心。
* 更改了 iwenchant 命令的功能，它现在会尝试附魔玩家手上的物品而非给予玩家一个新物品。
* 现在每个非基础心都有随材料等级变化的附魔能力
  * 基础心的附魔能力始终是定值，以与它们拥有固定的材料等级对应。
* 加强了无限诅咒能力，现在无限诅咒影响具有同名效果的生物时，会使效果立刻产生伤害并重新添加它。以前需要等待 5s 的不受伤时间。
* 修改了斩击能力，去除了流血效果，将能力消耗由 10 下调为 5。
* 重做了无限斩击能力，现在其拥有左键右键两个攻击模式，以融合其下位的斩击与连斩系列的特性。
* 圆锥形横扫攻击中，视为贴近玩家的距离从 0.1 更改为 0.5 格。
  * 目前影响斩击和剔骨系能力的攻击。
* 现在附魔能力会影响随机附魔。更高能力将意味着可能更高的附魔消耗上限。同时其也会影响符文锻造台附魔，更高附魔能力将使每个青金石具有更高价值。
* 微调符文锻造台附魔功能的部分参数。
  * 将每个青金石的基础价值从 200 下调到 160，将每级聚能的价值乘数增加从 1 下调到 0.5。
  * 现在消耗的青金石数量以向上取整而非向下取整计算。

#### Refactored

* 优化了使用能力工具相关 API 的调用逻辑。

#### Fixed

* 修复了符文附魔台预附魔时不对非基础心限制附魔等级的问题。
* 修复了能力条显示不完整的问题。
* 修复了在符文附魔台修复不能用任何物品修复的物品时会产生异常的问题。
* 使用符文锻造台附魔时，不再能通过叠加附魔得到超过当前世界等级的附魔等级。现在会显示 “对于你得到的附魔而言，世界等级太低”。
  * 目前这种情况与使用一个附魔等级超出世界等级的附魔符文不做区分。