package org.yang.interestingworld.particle;

public class CycleParticleEffect
{/* implements ParticleEffect
{

	public static final MapCodec<CycleParticleEffect> CODEC = RecordCodecBuilder.mapCodec(
			instance -> instance.group(Codec.FLOAT.fieldOf("scale").forGetter(particleEffect -> particleEffect.scale))
					.apply(instance, CycleParticleEffect::new));
	public static final PacketCodec<RegistryByteBuf, CycleParticleEffect> PACKET_CODEC = PacketCodec.tuple(
			PacketCodecs.FLOAT, effect -> effect.scale, CycleParticleEffect::new);
	private final float scale;

	public CycleParticleEffect(float scale)
	{
		this.scale = scale;
	}

	@Override
	public ParticleType<CycleParticleEffect> getType()
	{
		return IWParticleTypes.CYCLE_PARTICLE_TYPE;
	}*/
}
